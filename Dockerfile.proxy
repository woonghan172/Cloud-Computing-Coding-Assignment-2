FROM python:3.12-slim

WORKDIR /app

# Install both fastapi/uvicorn and the new httpx library
RUN pip install fastapi uvicorn httpx

COPY proxy.py .
COPY consistent_hashing.py .

CMD ["uvicorn", "proxy:app", "--host", "0.0.0.0", "--port", "8080"]